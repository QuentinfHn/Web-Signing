# ===========================================
# Environment Variables for LED Signage Controller
# ===========================================
# Copy this file to .env and fill in your values
# cp .env.example .env

# ===========================================
# Backend Configuration
# ===========================================

# Database URL for Prisma (SQLite)
# For local development (path relative to backend folder):
DATABASE_URL=file:./data/led.db
# For Docker (set in docker-compose.yml):
# DATABASE_URL=file:/app/data/led.db

# Node environment (development | production)
NODE_ENV=development

# Backend server port
PORT=8080

# Frontend URL(s) for CORS (comma-separated for multiple origins)
# For local dev: http://localhost:3000
# For production: https://yourdomain.com
# Multiple origins: https://domain1.com,https://domain2.com
FRONTEND_URL=http://localhost:3000

# ===========================================
# Authentication (REQUIRED)
# ===========================================

# Admin password for protecting admin pages and the Companion API
# REQUIRED: The server will not start without this set
# When set, /control, /content, /screens, /map and /api/companion require login
ADMIN_PASSWORD=your_secure_password_here

# JWT secret for session tokens (REQUIRED IN PRODUCTION!)
# Use a secure random string (32+ characters recommended)
# Generate with: openssl rand -base64 32
# In development, a default is used (WARNING: not secure!)
# JWT_SECRET=your_secure_random_secret_here_minimum_32_chars

# ===========================================
# Frontend Configuration (optional)
# ===========================================

# API URL (only needed if frontend runs separately from backend)
# VITE_API_URL=http://localhost:8080

# WebSocket URL (only needed if frontend runs separately from backend)
# VITE_WS_URL=ws://localhost:8080

# ===========================================
# VNNOX Player Management (optional)
# ===========================================

# Leave empty to disable VNNOX integration
# VNNOX_APP_KEY=your_app_key_here
# VNNOX_APP_SECRET=your_app_secret_here
# VNNOX_API_URL=https://open-eu.vnnox.com

# ===========================================
# Production / Cloudflare Tunnel (optional)
# ===========================================

# Cloudflare Tunnel token (get from Zero Trust dashboard)
# TUNNEL_TOKEN=your_tunnel_token_here
